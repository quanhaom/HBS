package frame;

import javax.swing.JFrame;

public class EditPro extends JFrame {
    private CustomerFrame customerFrame;

    // Constructor that accepts a CustomerFrame instance
    public EditPro(CustomerFrame customerFrame) {
        this.customerFrame = customerFrame;
        // Initialize the EditPro frame here
        setTitle("Edit Profile");
        setSize(400, 300);
        setDefaultCloseOperation(JFrame.DISPOSE_ON_CLOSE);
        setLocationRelativeTo(null);
        
        // Add your components and logic for editing profile here
    }

    // Additional methods for editing user profile...
}
